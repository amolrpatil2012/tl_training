function add(x) {
  console.log(x.length);
}

add("demo");
add(34.34);


// TS = JS + ES6 + Strict Type Checking

// 1. Convert Typescript into JavaScript [ transpliling / Transpiler]

// 2. For any data type mismatch will get compile time error